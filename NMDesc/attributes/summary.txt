all attributes list here

The gene level features include:
PPI, motifs, connectivity, interpro annotation, pfam domain, exon count,tau, nuclear localization signal, enrichment, protein interaction interface.

The variant level features include: 
fasta sequence, parse score, ppi, pfam, distance from ptc to cds end
